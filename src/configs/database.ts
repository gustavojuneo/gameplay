const DATABASE_NAME = '@gameplay';

//@app:collection
const COLLECTION_USERS = `${DATABASE_NAME}:user`;
const COLLECTION_APPOINTMENT = `${DATABASE_NAME}:appointment`;

export { COLLECTION_USERS, COLLECTION_APPOINTMENT };
